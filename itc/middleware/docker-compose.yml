services:
  chatdb-middleware-itc:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    image: chatdb-middleware-itc:latest
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8001:8001"
    networks:
      - chat-db

networks:
  chat-db:
    external: true
