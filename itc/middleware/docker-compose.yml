services:
  chatdb-middleware-itc:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    image: chatdb-middleware-itc:latest
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - chat-db

networks:
  chat-db:
    external: true
