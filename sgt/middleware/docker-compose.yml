services:
  chatdb-middleware-sgt:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    image: chatdb-middleware-sgt:latest
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - chat-db

networks:
  chat-db:
    external: true
