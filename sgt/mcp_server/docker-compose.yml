services:
  chatdb-mcp-server-sgt:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    image: chatdb-mcp-server-sgt:latest
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - chat-db

networks:
  chat-db:
    external: true
